const getters = {
  packages: (state) => state.packages,
  entities: (state) => state.entities,
  path: (state) => state.path,
  selectedEntityTypeIds: (state) => state.selectedEntityTypeIds,
  selectedPackageIds: (state) => state.selectedPackageIds
}
export default getters
